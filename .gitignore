# Ignore all hidden files and folders
.*
public/
dist/
node_modules/

# But DO NOT ignore the .gitignore file itself
!.gitignore

# Optional: Allow other specific hidden files you might need
# !.github/
# !.env.example